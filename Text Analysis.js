const message = 'გამარჯობა';


let counts = [0, 0, 0];
for(let i = 0; i < message.length; i++) { if (
    message[i] === 'რ')
    counts [0] ++
    else if (
        message [i]  === 'ა')
        counts [1] ++
        else if (message[i] === ',')
        counts[2] ++
} 

